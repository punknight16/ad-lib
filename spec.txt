lets make a quick app.

for config it needs:
	1) a git repository -done
	2) a heroku server -done
	3) a spec - done

for features it needs:
	0) templates in data.js
	1) accept form input
	2) place the input into a random template
	-----stretch goals-----
	3) parse data from an uploaded file
	4) input the parsed data into a random template

files
	routes.js - done
		get.ViewOne
		get.ViewAll
		post.CreateOne
		post.EditOne
		post.DestroyOne
	data.js - done
		templates
		log
	interactors - done
		createAdLib.js
	view-controllers
		entries.jsx
		entry.jsx
	view-models
		entries-display.jsx
		entry-display.jsx
		entry-intake.jsx
		entry-editor.jsx
	view-xhandlers
		getEntriesFromServer
		getEntryFromServer
		postEntryToServer